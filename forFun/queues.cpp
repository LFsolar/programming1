/*
	Make a queue
*/

#include <iostream>

using namespace std;

// Need i from main to use in push()
// structs can be declares outside of main
struct Node {
	int info = 0;
	Node * next;
} i;

// Add number to queue
void push(int num) {
	i;
}

int main() {
	int n = 5;
	int arr[n];
	int oldest;
	int newest;
	int i;
	
	// Make a circular array
	
	
	push(1);
	return 0;
}